---

  hosts: all
  gather_facts: true # this will make not to gather facts before running tasks

  tasks: 
    - name: stopservices
      win_service:
        name: {{vars_file}}
        state: stopped
      

  vars_file:
  servicesname.yml
        

    





