---

  hosts: all
  gather_facts: true # this will make not to gather facts before running tasks

  tasks: 
    - name: stop services
      win_service:
        state: stopped
      

  vars_file:
  servicesname.yml
        

    





