---

  hosts: all
  #gather_facts: true # this will make not to gather facts before running tasks

  tasks: 
    - name: stopservices
      win_service:
      state: stopped
      
  vars_files:
    - servicesname.yml
        

    





